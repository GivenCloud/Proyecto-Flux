apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- helmrelease.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: redis-values
  files:
  - values.yaml