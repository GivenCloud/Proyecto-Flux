apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: demo
spec:
  interval: 1m
  chart:
    spec:
      chart: ../../../../charts/redis
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  valuesFrom:
  - kind: ConfigMap
    name: redis-values
    valuesKey: values.yaml