apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
- helmrelease.yaml

configMapGenerator:
- name: backend-values
  files:
  - values.yaml